# ⚙️ CONEXIÓN AL BACKEND (BookMyBackend · NestJS + Prisma)
# URL base del API (Swagger en {URL}/api). OBLIGATORIA:
# toda la información del panel proviene exclusivamente del API.
NEXT_PUBLIC_API_URL=YOUR_API_URL
